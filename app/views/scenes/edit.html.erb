ここはscenes/edit.html.erbです。

<div class="container-fluid">
<div class="row">
	<div class="col-12">
		<%= react_component("Master2",[
			{serif:@scene.serifs.first.serif, 
			top:@scene.serifs.first.top, 
			left:@scene.serifs.first.left, 
			img_url:attachment_url(@scene.images.first, :image)},

			{serif:"セリフ2です", 
			top:100, 
			left:100, 
			img_url:attachment_url(@scene.images.first, :image)},

			{authenticity_token:form_authenticity_token}
			])%>


	</div>
</div>
</div>

